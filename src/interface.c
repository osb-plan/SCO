#include <stdio.h>
#include "generic_defs.h"

void _printc(char * out, char * msg, char * color)
{
    printf("Size of input msg = %lu\n", sizeof(msg));
    //char *out_msg = malloc(sizeof(*msg));
    //strncat(
    //fprintf(out,msg

}

